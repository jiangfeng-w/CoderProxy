# 登录与网关探针（M1）

- **状态**：已实现（2026-09-03）
- **优先级**：门槛验证
- **来源**：开发计划 §3.4；无竞品参考
- **定稿日期**：2026-09-03

## 需求背景

写整套 relay 前，最大不确定点是**牛码网关对"未知/长尾工具名"的容忍度**——参考实现 因自家工具都套好映射可以舍弃长尾，relay 面对任意 agent 的非固定工具，舍弃=能力残缺。必须先用最小探针实测，避免架构搭在错误假设上。

## 探针与结论

写法：登录拿 token → 裸发 3 个最小 `chat.completions`（HTTP 直连，不调用模型干活）。实测模型 `glm-5.3-flash`，登录走银海通 IM 静默（PKCE 兜底），chat 用模型自带 `llm-` key。

| 探针 | HTTP | 实测 | 结论 |
|---|---|---|---|
| A. 原生工具 `Bash` | 200 | 模型真返回 `tool_calls: Bash({"command":"echo hi"})` | 登录 + 模型 key + 工具调用链路通 |
| B. 未知工具 `glob_file_search` | 200 | 模型照常应答，self-describe 有文件搜索工具 | **网关容忍任意工具名，不拒** |
| C. 无工具 | 200 | 正常回 "ok"（含 `reasoning_content`） | 基础调用可用 |

**结论写回开发计划 §3.4**：牛码网关校验 schema 形状而非工具名 → hybrid 长尾默认 = 透传保留。

## 顺带定位的三个实现坑

1. **目录端点**：可用端点是 `POST /ai/continue/ide/list-assistants?appId=personal`（返回裸数组）；org/assistant 的 GET 端点实测 404/418。
2. **chat 鉴权**：走**模型自带 `apiKey`（`llm-…`）**，非 oauth 会话 token；`Authorization: Bearer <llm-…>` + `api-key: <llm-…>`。
3. **直连**：本机 httpx 默认读系统代理会把 `lc.yinhaiyun.com` 走隧道失败 → 必须 `trust_env=False`。

## 验收

- [x] M1 探针脚本跑通：登录（IM + PKCE 双路径）→ 3 个探针全部 HTTP 200
- [x] 得到「容忍未知工具名」的确定性结论，已回填开发计划 §3.4 与 M2/M3 默认值